# Websites

This is a monorepo for all of my websites which are on \*.tigertaylor.xyz. It is managed using pnpm and turbo.

## Structure

- `apps/`: This directory contains all of the applications (websites) that I have. Each application is in its own subdirectory.
- `packages/`: This directory contains any shared packages or libraries that are used across multiple applications

## Licenses

- Each application may have its own license, which can be found in the respective application's directory.
- Unless otherwise stated, assume there is no license to use the code in this repository.
